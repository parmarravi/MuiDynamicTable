import { Typography } from "@mui/material";

export default function Home() {
  return (
    <div>
      <Typography variant="h4">Welcome to the Admin Panel</Typography>
      <Typography>
        Use the sidebar to navigate through different sections of the panel.
      </Typography>
    </div>
  );
}
